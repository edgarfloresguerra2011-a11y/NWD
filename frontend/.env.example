# Para desarrollo local (npm run dev)
VITE_API_URL=http://localhost:8000/api/v1

# Para producción con Docker, el nginx hace proxy — no se necesita esta variable
# VITE_API_URL=http://backend:8000/api/v1
